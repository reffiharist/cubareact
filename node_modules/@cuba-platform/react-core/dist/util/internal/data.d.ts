import { WithId } from '../metadata';
import { SerializedEntityProps, MetaClassInfo } from '@cuba-platform/rest';
/**
 *
 * @param entityInstance
 * @param entityName
 * @param metadata
 *
 * @returns a shallow copy of a provided entity instance with temporary ids and read-only properties removed
 * (applied recursively to nested entities)
 */
export declare function prepareForCommit<T, U extends T & Partial<SerializedEntityProps> & WithId>(entityInstance: U, entityName: string, metadata: MetaClassInfo[]): U;
