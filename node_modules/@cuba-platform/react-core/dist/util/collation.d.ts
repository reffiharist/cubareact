import { SerializedEntity } from '@cuba-platform/rest';
/**
 * Performs client-side sorting of entities, mimicking the way sorting is performed by REST API
 *
 * @param items - entity instances array
 * @param sort - REST API parameter representing sort field and order
 */
export declare function sortEntityInstances<T>(items: Array<SerializedEntity<T>>, sort: string | undefined): Array<SerializedEntity<T>>;
export declare function defaultCompare(valA: any, valB: any, sortOrderModifier?: -1 | 1): number;
