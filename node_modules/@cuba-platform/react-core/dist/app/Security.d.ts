import { ObservableMap } from 'mobx';
import { CubaApp, EntityAttrPermissionValue } from '@cuba-platform/rest';
export declare class Security {
    private cubaREST;
    attrPermissionCache: ObservableMap<string, EntityAttrPermissionValue>;
    private effectivePermissions?;
    private restSupportEffectivePerms;
    permissionsRequestCount: number;
    constructor(cubaREST: CubaApp);
    get isDataLoaded(): boolean;
    getAttributePermission: (entityName: string, attributeName: string) => EntityAttrPermissionValue;
    canUploadAndLinkFile: () => boolean;
    loadPermissions(): Promise<void>;
}
